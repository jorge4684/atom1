
const range1=document.getElementById("range1");
const range2=document.getElementById("range2");

range1.addEventListener = (move) =>{
    labelMonto.innerHTML = 0;
    range1.innerHTML = "";
 if( labelMonto === 0) {
    return labelMonto.innerHTML;
    }else{
        return labelMonto.value.innerHTML;   
    }; 
}; 